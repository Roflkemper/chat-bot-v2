# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Secrets & local config
.env
.env.local
bot_local_config.json
*.pem
*.key
# Unknown / legacy
.sixth/

# GinArea tracker output
ginarea_live/
ginarea_tracker/.env

# Cleanup artifacts
cleanup.log
CLEANUP_*.bat.bak

# Live state
state/regime_state.json
state/bot_manager_state.json
state/personal_bot_learning.json
state/transition_alert_state.json
state/grid_portfolio.json
state/calibration/

# Logs
logs/
*.log
.sixth/
# Backtest reports
backtests/backtest_*_report.json
backtests/freeze_data_*.json

# Delivery / temp
TZ-*_delivery.zip
state_snapshot.zip
*.tmp

# Runtime reports
reports/

# Critical docs must NEVER be excluded by gitignore rules (TZ-055)
!docs/MASTER.md
!docs/PLAYBOOK.md
!docs/OPPORTUNITY_MAP_v1.md
!docs/GINAREA_MECHANICS.md
!docs/SESSION_LOG.md

# TZ-068: state snapshot artifacts (generated, not committed)
docs/STATE/CURRENT_STATE_*.md
docs/STATE/state_latest.json
docs/STATE/dashboard_state.json
docs/state_inline.js
state/runtime_state_snapshot.lock

# TZ-LIQ-INGESTION-90D: historical liquidation data (fetched, not committed)
market_data/liquidations_historical/

# TZ-PROJECT-MEMORY-DEFENSE: project map (generated, not committed)
docs/STATE/project_map.json
docs/STATE/PROJECT_MAP.md

# TZ-CONFLICTS-TRIAGE: machine-readable triage (generated, not committed)
docs/STATE/conflicts_triage_*.json

# TZ-SIGNAL-LOGGER / TZ-GITIGNORE-ADVISE-RUNTIME: runtime JSONL logs (not committed)
state/advise_signals.jsonl
state/advise_null_signals.jsonl
state/advise_action_match.jsonl
state/advise_followup.jsonl

# Local Claude Code settings (operator-specific permissions)
.claude/settings.json
.claude/scheduled_tasks.lock

# Diagnostic / collection dumps (one-off, never commit)
_baseline_errors*.txt
_collect_*.txt
_tz044_*.txt

# Disaster-recovery snapshots (kept on disk, not in repo — see _recovery/incident_report_docs.md)
_recovery/

# Frozen OHLCV market data (large CSVs — fetched per session, not committed)
backtests/frozen/*.csv
backtests/frozen/*.csv.bak*
backtests/frozen/1y_ingest/

# Regime calibration snapshots (high-frequency diagnostic dumps, ~80/day)
data/calibration/regime_markdown_*.json
data/calibration/regime_markup_*.json
data/calibration/regime_range_*.json

# Runtime collector output (PIDs, logs, CSV streams from live collectors)
run/
ginarea_tracker/run/
market_collector/run/
market_live/
market_live_dev/
market_live_profiler/
features_out/
