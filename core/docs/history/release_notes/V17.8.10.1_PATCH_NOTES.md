# V17.8.10.1

## Что сделано
- runtime state файлы вынесены из Git через `.gitignore`
- `PUSH_UPDATE.bat` теперь делает `git pull --rebase origin main` перед `push`
- в push-процессе автоматически убираются из Git index:
  - `storage/market_state.json`
  - `storage/position_state.json`
  - `storage/journal/*`
  - `logs/*.log`
- добавлен `CLEAN_RUNTIME_STATE_FROM_GIT.bat` для разовой очистки индекса

## Цель
- убрать конфликты по runtime state
- убрать ошибку `fetch first`
- стабилизировать zero-click push
