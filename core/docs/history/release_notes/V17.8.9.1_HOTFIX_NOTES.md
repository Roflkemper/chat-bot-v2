# V17.8.9.1 HOTFIX

- Исправлен runtime-crash в `core/pipeline.py`: переменная `execution_side` была не определена при сборке блока `ginarea`.
- Логика переведена на уже существующую рабочую сторону `active_side`, которая и сохраняется в snapshot как `execution_side`.
- Обновлён `PROJECT_MANIFEST.md` и добавлена синхронная копия в `docs/PROJECT_MANIFEST.md` для GitHub workflow `build_context.yml`.
- Добавлен регрессионный тест на сборку snapshot без этого падения.
