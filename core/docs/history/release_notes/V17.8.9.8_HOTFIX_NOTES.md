# V17.8.9.8 HOTFIX

- Возвращён жёсткий trigger gate: без текущего trigger `PREPARE/ENTER` запрещены.
- `trigger_detected` теперь берётся из текущего `detect_trigger()`.
- Без trigger entry-блок остаётся `NO_TRADE / NO_ENTRY`.
- Добавлено предупреждение для `PROBE` при `DEEP` глубине.
