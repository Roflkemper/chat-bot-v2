# V17.8.9.2 PATCH NOTES

## FIXED
- Исправлен zero-click push/runtime-сценарий для Git, который падал на `fatal: cannot rename the current branch while not on any`.
- `PUSH_UPDATE.bat` теперь автоматически:
  - пытается прервать незавершённый `rebase` / `merge`;
  - корректно восстанавливает рабочую ветку `main`, если репозиторий оказался в detached HEAD;
  - пушит явно в `origin main`.
- `INIT_GITHUB_PRIVATE_REPO.bat` усилен той же логикой восстановления ветки и очистки незавершённого git-состояния.

## RESULT
- zero-click push больше не должен ломаться на попытке `branch -M` в состоянии detached HEAD;
- сценарий инициализации и повторного пуша стал устойчивее после прерванных rebase / merge.
