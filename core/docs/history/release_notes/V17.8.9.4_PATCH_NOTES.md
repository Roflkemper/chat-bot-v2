# V17.8.9.4

- При `MID_RANGE` trigger теперь сбрасывается полностью.
- При `MID_RANGE` context скрывается и не рендерится как рабочий входной контекст.
- Renderer больше не показывает фантомный `RECLAIM` вне рабочей зоны блока.
