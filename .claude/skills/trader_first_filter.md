---
# Skill: trader first filter

## For whom
Этот skill применяется ARCHITECT'ом (Claude) ПЕРЕД нарезанием
любого TZ для Code или Codex. Self-check на trader-value
оправдание любой инженерной работы.

## Trigger
Architect готов оформить TZ. Применяется ВСЕГДА — без
исключений по типу задачи. Особенно строго для:
- Новые модули, рефакторы, API-обвязка
- Любой TZ, оформление которого занимает >30 мин
- TZ инициированные Claude-ом (не оператором)

## Rule
TZ должен явно проходить **минимум одну** из трёх категорий:

- **(а) лучшее risk-profile в реальной торговле** — изменение
  поведения советника/исполнителя приведёт к меньшим
  потерям/большему edge на real money.

- **(б) тестирование hypothesis на real data** — backtest,
  reconcile, calibration, anomaly detection на исторических
  снимках/OHLCV. Hypothesis должна быть сформулирована
  ДО запуска (не fishing).

- **(в) защита капитала от bugs** — fix bug, который мог бы
  привести к ложному сигналу /advise, неверному отчёту,
  неконтролируемой потере данных, утечке памяти ломающей
  суточный uptime.

## Mandatory steps BEFORE отправки TZ оператору

1. Architect формулирует TZ полностью.

2. Записывает в TZ-блок (или явно фиксирует у себя) категорию
   из (а)/(б)/(в) + одну строку обоснования с конкретикой:
   - (а): какой именно сигнал/решение улучшится, на сколько $/год
   - (б): что именно проверяется, какой dataset, какой decision
     accept/reject выйдет из результата
   - (в): какой баг, какой ущерб без fix'а, кто видел повторное
     проявление

3. Если ни одна категория не проходит без натяжки — STOP.
   Не отправлять. Варианты:
   - Запросить у оператора явное одобрение с фразой "сделай
     это инженерное упражнение" (рефакторы, cleanup долгов,
     dev-experience improvements)
   - Положить в backlog как DEBT с classification
   - Закрыть задачу совсем

## Forbidden
- Маскировать DEBT cleanup / refactoring под (а)/(б)/(в).
  "Чище код → быстрее найдём bug → защитим капитал" — это
  третий-четвёртый уровень связи, не trader-first.
- Скрывать (б)-задачу без сформулированной hypothesis. Если
  не известно что именно ищем — это exploration, не testing.
- Оформлять TZ под лозунгом "infrastructure" без явного указания
  какой trader-action эта infrastructure разблокирует.

## Allowed
- TZ формально категории (в) с малым ожидаемым ущербом, если
  fix дешевле discovery (правка одной строки, явный typo).
- TZ инициированный оператором — фильтр всё равно применяется,
  но оператор может явно указать "делаем для удобства" и
  закрыть категорию через operator-approval строку.

## Recovery
Если architect отправил TZ без trader-first обоснования и оператор
указал на это:
- Зафиксировать как self-check failure в INCIDENTS.md (или памяти
  через memory rule)
- Не "переоформлять" задачу натянутой категорией. Либо явно
  получить operator-approval, либо отозвать TZ.

## Why
Память проекта несколько раз фиксировала: Claude склонен генерировать
TZ "потому что технически интересно" — рефакторы, абстракции, новые
hypothetical features. Это съедает оператора и contextual budget.
Trader-first filter — единственный механизм который не даёт engineering
интересам перевешивать торговые приоритеты.

PROJECT_CONTEXT §9 определяет это как HARD rule на каждый TZ.
