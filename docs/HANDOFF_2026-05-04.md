# Claude Session Handoff — 2026-05-04 / week 2 close

> Paste this into a new Claude chat as first message. Coordinator gets full project context for week 3 entry.

---

## §0 Quick orient (read first)

- **Test verification (this session, exact command from close brief):**
  ```
  python -m pytest core/tests/test_dedup_position_change_wire.py core/tests/test_dedup_boundary_breach_wire.py core/tests/test_position_dedup.py -q
  → 35 passed in 1.16s ✅
  ```
- **Live production changes:**
  1. POSITION_CHANGE dedup wrapper wired in `DecisionLogAlertWorker` (env flag `DEDUP_LAYER_ENABLED_FOR_POSITION_CHANGE` default ON; cooldown 300s, value Δ 0.05 BTC)
  2. BOUNDARY_BREACH dedup wired with cluster-collapse (60s window, 0.5% price Δ)
  3. Dashboard position dedup fixed: `shorts.total_btc` was `-2.241` BTC (~73% inflation), now `-1.296` BTC (matches operator's BitMEX UI exactly). Bot_id `.0` legacy suffix normalization in `_read_csv_latest_by_bot`.
- **Next operator action — 24h monitoring per `docs/OPERATOR_DEDUP_MONITORING.md`:** confirm POSITION_CHANGE alerts still arrive, no >5min duplicates, ≥0.05 BTC moves always emit, per-bot independence holds.

---

## §1 Last 16 closed CPs (2026-05-05)

| CP | TZ | Result |
|----|-----|--------|
| CP15-17 | TZ-RGE-RESEARCH-EXPANSION | 5×3 expansion variant matrix, 0.9s; B variant DP-001-visible |
| CP18 | TZ-LONG-TP-SWEEP | 5 TP × 4 windows; net PnL monotonic with TP ($564→$1,476) |
| CP19 | TZ-BACKTEST-AUDIT | Trust map: 0 prod-aligned / 7 partial / 1 default / 2 signal-side |
| CP20 | TZ-BACKTEST-DATA-CONSOLIDATION | 17 GinArea BTs structured BT-001..017 |
| CP21 | TZ-REGIME-CLASSIFIER-PERIODS-ANALYSIS | RANGE 72%, ZERO direct trend↔trend transitions, 645 episodes |
| CP22 | TZ-DEDUP-DRY-RUN-PRODUCTION-LOG | BOUNDARY 95% TOO AGGRESSIVE; POSITION_CHANGE 10.6% HEALTHY |
| CP23 | TZ-DASHBOARD-LIVE-FRESHNESS | 60s loop + 3-tier freshness layer |
| CP24 | TZ-REGIME-OVERLAY | BT × regime PnL allocation table (96-99% coverage) |
| CP28 | TZ-CP28-JOINT-FINDINGS | Findings A/B/C consolidated by operator+MAIN |
| CP30 | TZ-K-DUAL-MODE-COORDINATOR-DESIGN | docs/DESIGN/P8_DUAL_MODE_COORDINATOR_v0_1.md (12 sections) |
| CP31 | TZ-CROSS-CHECK-FINDING-A | Verdict A: sign-flip survives period correction (~0.40 BTC swing) |
| CP32 / CP-Y | TZ-DASHBOARD-POSITION-DEDUP | shorts.total_btc fixed −2.241→−1.296 |
| CP-G | TZ-DEDUP-WIRE-PRODUCTION (POSITION_CHANGE) | Wired + 12 tests + 24h monitoring required |
| CP-G2 | TZ-DEDUP-WIRE-BOUNDARY_BREACH | Cluster-collapse + 10 tests |
| CP-H | TZ-DASHBOARD-FRESHNESS-FINALIZE | D77 closed: snapshots.csv accepted as v1 source |

Plus week-2 P1 chain (3 TZs), P2 BOT-INVENTORY, P4 DASHBOARD-PHASE-1, P6 (MORNING-BRIEF + BOT-ALIAS-HYGIENE), P7 (TELEGRAM-INVENTORY + ALERT-DEDUP-LAYER + METRICS-RENDER-FIX), P8 (RANGE-DETECTION).

---

## §2 Live production changes (operator-visible)

1. **Dashboard positions now match BitMEX reality.** Pre-fix the dashboard summed each bot twice (legacy `5196832375.0` + modern `5196832375` rows treated as distinct). Post-fix (`_normalize_bot_id`) collapses both formats. Operator can now compare dashboard `shorts.total_btc` directly to BitMEX UI without mental adjustment.
2. **POSITION_CHANGE Telegram alerts go through state-change + cooldown gate.** Same alert at same position size within 5 min is suppressed. Δ ≥ 0.05 BTC re-emits after cooldown expires. Per-bot independence preserved.
3. **BOUNDARY_BREACH Telegram alerts cluster-collapse.** Multiple breaches at close prices (within 0.5%) within 60s window emit as one consolidated message instead of N separate.
4. **Dashboard freshness banner** — when snapshots stale or forecast >2h old, banner turns yellow / red with explanation in Russian.
5. **Bot registry** — 22 GinArea bots have stable UIDs in `data/bot_registry.json`; resolver in `services/bot_registry/`.

---

## §3 24h operator monitoring required

Per `docs/OPERATOR_DEDUP_MONITORING.md`, confirm during the next 24-48h:

- [ ] POSITION_CHANGE alerts still arrive (not silenced)
- [ ] No duplicates within 5 min for same bot at same position size
- [ ] Position changes ≥0.05 BTC always emit after cooldown
- [ ] Per-bot independence (TEST_1 vs TEST_2 don't cross-suppress)
- [ ] BOUNDARY_BREACH cluster collapse working (1 message per cluster, not N)

**Rollback if issues:** set `DEDUP_LAYER_ENABLED_FOR_POSITION_CHANGE=0` in env, restart worker. State file at `data/telegram/dedup_state.json` is safely deletable.

---

## §4 Top 3 priority TZs for week 3

| Rank | TZ | Why first |
|------|-----|-----------|
| 1 | **TZ-TRANSITION-MODE-COMPARE-BACKTEST** | Closes P8 §9 Q2 (TRANSITION_MODE policy). Operator-side GinArea backtest. Until this resolves, P8 implementation TZ stays gated. |
| 2 | **TZ-PURE-INDICATOR-AB-ISOLATION** | Closes Finding A's confounded variables. BT-014..017 *without* indicator on same 86-day window. Operator GinArea run. |
| 3 | **TZ-K-RECALIBRATE-PRODUCTION-CONFIGS** | Re-runs `direct_k` with live configs (size 0.001 BTC / $100, count 200/220, indicator+instop where applicable). Closes audit rows 1-6. Script ready (`scripts/reconcile_direct_k.py`); ~30 min compute. |

#4 onward: dedup wrapper expansion (PNL_EVENT, then PNL_EXTREME), CSV consumers audit, broken test cleanup, Impulse trigger recalibration, self-regulating bot research, low-priority dashboard validation.

---

## §5 Operator decisions made / pending

### Sizing v0.1 — Q1-Q5 closed (locked 2026-05-05 in design doc)

| Q | Decision |
|---|----------|
| Q1 base values | 1.4 / 1.0 / 0.6 confirmed |
| Q2 WR threshold | 10 decided trades (was 5) |
| Q3 conflict cap | 0.5 (soft, not 0.0) |
| Q4 reasoning language | Russian only |
| Q5 output channels | Brief + dashboard, NOT Telegram (deferred) |

### P8 coordinator §9 — Q1-Q5 partial (MAIN-defaults stand pending operator confirm)

| Q | MAIN default | Status |
|---|--------------|--------|
| Q1 SHORT cumulative cap | 3.0 BTC | Pending operator confirm |
| Q2 TRANSITION_MODE policy | All bots paused | Pending Q2 backtest (week 3 priority 1) |
| Q3 keep impulse_long_rej in catalog | Yes | Pending operator confirm |
| Q4 killswitch threshold | 5% free margin | Pending operator confirm |
| Q5 tick interval | 60s | Pending operator confirm |

---

## §6 Pre-existing technical debt (surfaced this session)

`TZ-FIX-COLLECTION-ERRORS` covers:

- 2 broken test files at full-pytest-collection time:
  - `core/tests/test_backtest_engine.py`
  - `core/tests/test_telegram_runtime_state.py`
- These cause `pytest core/tests/` (no specific files) to abort with collection errors. Working around it requires running specific test files (which is what `done.py`-validated commands have been doing all session).
- Brittle datetime tests elsewhere in core/tests/ — failures depending on local timezone or test-runner clock drift; flagged for cleanup.
- DEBT-04 / 91 collection errors (long-standing) — separate split plan exists in PENDING_TZ.

These do **not** affect the production wire-ups committed this session — those were validated via narrow file-list pytest commands per the close brief.

---

## §7 Test verification

Exact command from close brief:
```
python -m pytest core/tests/test_dedup_position_change_wire.py core/tests/test_dedup_boundary_breach_wire.py core/tests/test_position_dedup.py -q
```

Result:
```
35 passed in 1.16s
```

Breakdown: 12 (POSITION_CHANGE wire) + 10 (BOUNDARY_BREACH wire) + 13 (position dedup). All 35 green.

Full pipeline tests across all session-closed modules also confirmed green earlier in the session at 235/235.

---

## §8 Reference for new chat

When starting next chat, paste this handoff + the standard `HANDOFF_2026-05-03.md` (Project Context part). Then:

1. Coordinator reads `docs/CONTEXT/STATE_CURRENT.md` for fresh phase status
2. Reads `docs/STATE/PENDING_TZ.md` "NEXT WEEK" section for week 3 queue
3. Reads `docs/PLANS/MULTI_TRACK_ROADMAP.md` for track-status snapshot
4. Reviews `docs/CONTEXT/DRIFT_HISTORY.md` DRIFT-016..022 + META-PATTERN-001 (anti-patterns from this session)

For week 3 entry, the first TZ should typically be one of the top-3 priority items in §4 above. Run `python scripts/main_morning_brief.py --roadmap docs/PLANS/MULTI_TRACK_ROADMAP.md --day YYYY-MM-DD --dry-run` to generate a working sprint draft from the roadmap.

---

## §9 Skills, working agreements, and behavioral guards (carried forward)

- Operator does not run heavy simulations; coordinator delegates compute to Codex tier when batch-numerical, keeps foundational analytical work in Claude tier (DRIFT-022).
- Bash(*)/PowerShell(*) globally allowed; no permission prompts.
- Telegram done.py notification after each closed TZ — operator reads TG, not terminal.
- "Pause beats close" (operator P0) is a project-wide invariant.
- Audit/diagnosis recommendations must become PENDING_TZ entries before audit closes (DRIFT-020).
- UI deliverables need visual confirmation before CP GREEN (DRIFT-018).
- Always cite live config from `MASTER §6` / `GINAREA_MECHANICS §6` before specifying any "default" parameter (DRIFT-019).

---

## §10 Last commit

`2502095` — "feat: 4 blocks — backtest registry + regime periods + dedup dry-run + dashboard freshness"

Plus subsequent block-13/14 + TZ-K + TZ-Y + TZ-G/G2/H + cross-check + position dedup commits. Branch: `tz-session-close-proper-handoff-2026-05-03` (still active; week 3 should branch off it or merge to main first per operator decision).

End of handoff.
