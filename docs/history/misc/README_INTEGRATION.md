Этот архив — overlay-пакет HARD CUTOVER RELEASE.

Он даёт готовый новый источник истины: `execution_snapshot`.
Если после подключения вывод всё ещё показывает `QUIET / PAUSE` и `NEUTRAL`, значит проект продолжает рендерить legacy-блоки.
