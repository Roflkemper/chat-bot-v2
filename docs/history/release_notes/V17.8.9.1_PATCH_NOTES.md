# V17.8.9.1 PATCH NOTES

- fixed runtime crash in `core/pipeline.py`
- restored local `execution_side` binding before GinArea grid state build
- updated `PROJECT_MANIFEST.md` with ZIP-only delivery and no manual code-insert rules
- added regression test for `build_full_snapshot()` runtime path
