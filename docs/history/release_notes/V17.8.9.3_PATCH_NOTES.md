# V17.8.9.3

- Исправлен resolver: `AGAINST_MID + STRONG` теперь не блокирует trigger и даёт `PREPARE`.
- Для `AGAINST_LOW` trigger больше не блокируется.
- `WATCHING` теперь сохраняется при `consensus_alignment >= 2/3`.
- Исправлен порядок для GINAREA lifecycle: `WATCHING -> HOLD_GRID` имеет приоритет над `RISK/DEEP -> REDUCE_GRID`.
- При `PREPARE` больше не должен рендериться `ENTRY: NO_TRADE`.
