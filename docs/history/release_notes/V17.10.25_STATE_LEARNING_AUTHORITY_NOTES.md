# V17.10.25 — STATE & LEARNING AUTHORITY

- trade_journal final close now persists closed trades into `state/trade_journal.jsonl`
- duplicate closed-trade journal rows are prevented by `trade_id`
- closed trade persistence now auto-updates `state/personal_bot_learning.json`
- setup stats now expose recent win/loss streaks and dominant recent exit reason
- learning adjustment now penalizes consecutive losses and invalidation-driven exits
- trade manager learning block now shows streak and recent exit profile
- added tests for closed-trade persistence and streak-aware learning adjustment
