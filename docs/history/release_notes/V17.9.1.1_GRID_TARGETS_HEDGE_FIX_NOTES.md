# V17.9.1.1 — GRID TARGETS HEDGE FIX

## Что исправлено
- GRID VIEW больше не считает импульс до ближайшей локальной свечной структуры.
- Для расчёта `DOWN IMPULSE` и `UP IMPULSE` теперь используются `hedge_arm_down` и `hedge_arm_up` как основные целевые уровни, если они есть в snapshot.
- `PRIORITY SIDE` остаётся `NEUTRAL`, когда локально нет перекоса, даже если среднесрочный bias направленный.
- В GRID VIEW добавлена явная строка, что directional bias среднесрочный, а локально сетки могут оставаться нейтральными.

## Проверка
- Локально прогнаны тесты: `14 passed`
