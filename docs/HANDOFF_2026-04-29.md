# Handoff 2026-04-29 → новая сессия

## Что в проде сейчас (28.04 evening)
- ADVISOR v1 multi-asset на BTC/ETH/XRP (3 секции в /advise)
- Cascade на live parquet, fallback на build_full_snapshot
- Live features writer (delta_1h, consec_up/down, momentum, range, killzone) каждую минуту в features_out/{symbol}/{date}.parquet
- 5 supervised процессов: supervisor, app_runner, tracker, collectors, watchdog (cmdline matching через WMIC, memory monitor 300/500/800 МБ, auto-restart app_runner при 800+ МБ)
- Auto outcome reconciliation для /advise stats
- Streaming ParquetWriter в collectors (memory leak fix)
- Shell:Startup .bat для автозапуска при reboot

## Что в очереди к проверке
- PROD-CHECK 24h дедлайн ~29.04 (после TZ-046 рестарта):
  • bot7 status: 5 OK, uptime > 23h
  • app_runner RSS < 300 МБ
  • advisor_log.jsonl и advisor_outcomes.jsonl растут
  • Telegram /advise / /advise stats / /advise log отвечают
  • alarm 500 МБ не сработал
- TZ-029-C 24h validation коллекторов (~29.04 11:36 UTC):
  • parquet rows > 0 для каждой биржи и stream-типа
  • 0 reconnect storms
  • msg-rate в норме

## Очередь Code (после PROD-CHECK)
- Зависит от результатов PROD-CHECK + TZ-029-C
- Если оба PASS → ничего активного, ждём гипотез/задач от оператора
- Если что-то FAIL → отдельный TZ диагностики

## Очередь Codex
- TZ-040 real bot snapshots в What-If (next)
- После TZ-040 — пересмотр OPPORTUNITY_MAP с расширенными данными (Шаг 9 §12)

## Закрытые гипотезы 28.04
- P-13 LIQUIDITY-HARVESTER — rejected (hi-variance, win 2.91%)
- P-14 PROFIT-LOCK-RESTART — rejected (win 22.83%)

## Открытые гипотезы для будущего
- dump_critical asymmetry (P-6 vs P-7) — CI overlap, нужно больше n. Решение: TZ-040 real snapshots + потенциальный multi-asset What-If
- КЛОД_ИМПУЛЬС лонг-бот — 1 сработка из истории, нужно 20-30 для анализа

## Технический долг (backlog)
- TZ-DEBT-08 ProcessPoolExecutor WinError 5 на Windows
- 13 pre-existing test failures
- TZ-DEBT-02 re-arm в bt-симуляторе (отложен до Шага 5)
- Adaptive Grid Manager dry-run — нет отдельной документации

## Контекст для первого вопроса нового Claude
"Прочти MASTER.md, PLAYBOOK.md, OPPORTUNITY_MAP_v1.md,
последние 3 записи SESSION_LOG.md (включая 2026-04-29 финал),
HANDOFF_2026-04-29.md.
Подтверди в 5 строках:
1. Главная цель проекта
2. Текущая фаза по §2
3. Закрытые шаги §12
4. Какие ТЗ сейчас в работе у Code и Codex
5. Что следующее по плану."
