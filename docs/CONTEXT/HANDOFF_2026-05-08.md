# HANDOFF 2026-05-08

## Сессия закрыта

Большой батч из 11 фич реализован и закоммичен в две стадии:
- **4f3c846** — B1 (cascade alert) + B2 (paper trade auto-open) + D1 (risk score)
- **05a8c27** — A1+A2+A3+A4+C1+C2+C3+D2 (flip tracker, OI spike, multi-exchange OI, premium history, structural levels, volume profile, BTC.D, watchlist)

## Что добавилось в /advise (advisor_v2)
- 🎯 **RISK SCORE** — long_risk vs short_risk (0–10), доминирующая сторона + reasons
- **РЫНОК ГЛОБАЛЬНО** — BTC.D % + total mcap (CoinGecko /global)
- **ИСТОРИЯ FLIP / SPIKE** — funding/premium streak hours до последнего sign-change + OI Δ% за 15min
- **LONG/SHORT РЫНОК** — Binance + Bybit account ratio + top trader ratio + taker buy/sell

## Что добавилось в /momentum
- **Volume Profile** — 24h POC + 70% value area из market_1m.csv
- **Structure** — PDH/PDL/session breaks из forecast_features parquet

## Новые async loop'ы в app_runner
- `cascade_alert` (60s) — алерт при long/short cascade ≥5 BTC за 5 мин (high) или ≥2 BTC (medium); auto-open paper trade на edge с n=103, 73% pct_up
- `watchlist` (60s) — оператор задаёт правила через `/watch add <field> <op> <value>`

## /watch — поддерживаемые поля
funding, long_pct, short_pct, top_long_pct, top_short_pct, taker_buy, taker_sell, premium, oi_change_1h, btc_d, price, cascade_long_5min, cascade_short_5min

Команды:
- `/watch` — список правил
- `/watch add funding > 0.01` — добавить
- `/watch del <id>` / `/watch on/off <id>` — управление

State: `state/watchlist.json`. Dedup 30 мин per rule.

## Состояние процессов
- app_runner был DEAD 25+ мин (crash budget исчерпан в 21:25 UTC, supervisor disabled auto-restart)
- Поднят вручную (`python app_runner.py` в фоне) — `telegram_bot.start`, `cascade_alert.start`, `watchlist.start` все в логах
- managed=4 в supervisor (tracker, collectors, state_snapshot, supervisor сам) — app_runner отдельно

## Известные шумовые ошибки (не критично)
- `services/market_forward_analysis/bot_impact.py:177` — `ZeroDivisionError` когда `current_price=0`. Logged, не валит loop.

## Pending (не сделано, но не блокирует)
- Тест /watch end-to-end на реальном правиле
- Тест cascade_alert на живом cascade (нужен >5 BTC liquidation за 5 мин)
- Перезапуск supervisor чтобы он стал managed app_runner и сбросил crash budget
